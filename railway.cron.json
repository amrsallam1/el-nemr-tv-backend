{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan elnemr:sync-worker --type=all --pages=2 --limit=50 --no-interaction",
    "cronSchedule": "0 1 * * *",
    "restartPolicyType": "NEVER"
  }
}
