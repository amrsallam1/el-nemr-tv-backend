{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "php artisan migrate --force && php artisan optimize:clear",
    "startCommand": "sh -c 'php artisan queue:work --sleep=2 --tries=1 --timeout=7200 & exec php artisan serve --host=0.0.0.0 --port=$PORT'",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
